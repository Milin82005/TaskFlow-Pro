const button = document.getElementById("changeBtn");
const heading = document.getElementById("heading");

button.addEventListener("click", () => {
    heading.innerText = "Welcome to TECHSHIP Internship";
});