let totalClasses = 150;
let attendedClasses = 120;

let attendancePercentage =
    (attendedClasses / totalClasses) * 100;

console.log(
    "Attendance Percentage:",
    attendancePercentage + "%"
);

console.log("\n");
